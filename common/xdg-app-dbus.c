/*
 * Generated by gdbus-codegen 2.46.2. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "./common/xdg-app-dbus.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.XdgApp.SessionHelper
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:XdgAppSessionHelper
 * @title: XdgAppSessionHelper
 * @short_description: Generated C code for the org.freedesktop.XdgApp.SessionHelper D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-XdgApp-SessionHelper.top_of_page">org.freedesktop.XdgApp.SessionHelper</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.XdgApp.SessionHelper ---- */

static const _ExtendedGDBusArgInfo _xdg_app_session_helper_method_info_request_monitor_OUT_ARG_path =
{
  {
    -1,
    (gchar *) "path",
    (gchar *) "ay",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _xdg_app_session_helper_method_info_request_monitor_OUT_ARG_pointers[] =
{
  &_xdg_app_session_helper_method_info_request_monitor_OUT_ARG_path,
  NULL
};

static const _ExtendedGDBusMethodInfo _xdg_app_session_helper_method_info_request_monitor =
{
  {
    -1,
    (gchar *) "RequestMonitor",
    NULL,
    (GDBusArgInfo **) &_xdg_app_session_helper_method_info_request_monitor_OUT_ARG_pointers,
    NULL
  },
  "handle-request-monitor",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _xdg_app_session_helper_method_info_pointers[] =
{
  &_xdg_app_session_helper_method_info_request_monitor,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _xdg_app_session_helper_interface_info =
{
  {
    -1,
    (gchar *) "org.freedesktop.XdgApp.SessionHelper",
    (GDBusMethodInfo **) &_xdg_app_session_helper_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "session-helper",
};


/**
 * xdg_app_session_helper_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-XdgApp-SessionHelper.top_of_page">org.freedesktop.XdgApp.SessionHelper</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
xdg_app_session_helper_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_xdg_app_session_helper_interface_info.parent_struct;
}

/**
 * xdg_app_session_helper_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #XdgAppSessionHelper interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
xdg_app_session_helper_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * XdgAppSessionHelper:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-XdgApp-SessionHelper.top_of_page">org.freedesktop.XdgApp.SessionHelper</link>.
 */

/**
 * XdgAppSessionHelperIface:
 * @parent_iface: The parent interface.
 * @handle_request_monitor: Handler for the #XdgAppSessionHelper::handle-request-monitor signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-XdgApp-SessionHelper.top_of_page">org.freedesktop.XdgApp.SessionHelper</link>.
 */

typedef XdgAppSessionHelperIface XdgAppSessionHelperInterface;
G_DEFINE_INTERFACE (XdgAppSessionHelper, xdg_app_session_helper, G_TYPE_OBJECT);

static void
xdg_app_session_helper_default_init (XdgAppSessionHelperIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * XdgAppSessionHelper::handle-request-monitor:
   * @object: A #XdgAppSessionHelper.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-XdgApp-SessionHelper.RequestMonitor">RequestMonitor()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call xdg_app_session_helper_complete_request_monitor() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-request-monitor",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (XdgAppSessionHelperIface, handle_request_monitor),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

}

/**
 * xdg_app_session_helper_call_request_monitor:
 * @proxy: A #XdgAppSessionHelperProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-SessionHelper.RequestMonitor">RequestMonitor()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call xdg_app_session_helper_call_request_monitor_finish() to get the result of the operation.
 *
 * See xdg_app_session_helper_call_request_monitor_sync() for the synchronous, blocking version of this method.
 */
void
xdg_app_session_helper_call_request_monitor (
    XdgAppSessionHelper *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "RequestMonitor",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * xdg_app_session_helper_call_request_monitor_finish:
 * @proxy: A #XdgAppSessionHelperProxy.
 * @out_path: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to xdg_app_session_helper_call_request_monitor().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with xdg_app_session_helper_call_request_monitor().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_session_helper_call_request_monitor_finish (
    XdgAppSessionHelper *proxy,
    gchar **out_path,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(^ay)",
                 out_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_session_helper_call_request_monitor_sync:
 * @proxy: A #XdgAppSessionHelperProxy.
 * @out_path: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-SessionHelper.RequestMonitor">RequestMonitor()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See xdg_app_session_helper_call_request_monitor() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_session_helper_call_request_monitor_sync (
    XdgAppSessionHelper *proxy,
    gchar **out_path,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "RequestMonitor",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(^ay)",
                 out_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_session_helper_complete_request_monitor:
 * @object: A #XdgAppSessionHelper.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @path: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-XdgApp-SessionHelper.RequestMonitor">RequestMonitor()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
xdg_app_session_helper_complete_request_monitor (
    XdgAppSessionHelper *object,
    GDBusMethodInvocation *invocation,
    const gchar *path)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(^ay)",
                   path));
}

/* ------------------------------------------------------------------------ */

/**
 * XdgAppSessionHelperProxy:
 *
 * The #XdgAppSessionHelperProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * XdgAppSessionHelperProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #XdgAppSessionHelperProxy.
 */

struct _XdgAppSessionHelperProxyPrivate
{
  GData *qdata;
};

static void xdg_app_session_helper_proxy_iface_init (XdgAppSessionHelperIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (XdgAppSessionHelperProxy, xdg_app_session_helper_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (XdgAppSessionHelperProxy)
                         G_IMPLEMENT_INTERFACE (XDG_APP_TYPE_SESSION_HELPER, xdg_app_session_helper_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (XdgAppSessionHelperProxy, xdg_app_session_helper_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (XDG_APP_TYPE_SESSION_HELPER, xdg_app_session_helper_proxy_iface_init));

#endif
static void
xdg_app_session_helper_proxy_finalize (GObject *object)
{
  XdgAppSessionHelperProxy *proxy = XDG_APP_SESSION_HELPER_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (xdg_app_session_helper_proxy_parent_class)->finalize (object);
}

static void
xdg_app_session_helper_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
xdg_app_session_helper_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
xdg_app_session_helper_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_xdg_app_session_helper_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], XDG_APP_TYPE_SESSION_HELPER);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, XDG_APP_TYPE_SESSION_HELPER);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
xdg_app_session_helper_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  XdgAppSessionHelperProxy *proxy = XDG_APP_SESSION_HELPER_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_xdg_app_session_helper_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_xdg_app_session_helper_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
xdg_app_session_helper_proxy_init (XdgAppSessionHelperProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = xdg_app_session_helper_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, XDG_APP_TYPE_SESSION_HELPER_PROXY, XdgAppSessionHelperProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), xdg_app_session_helper_interface_info ());
}

static void
xdg_app_session_helper_proxy_class_init (XdgAppSessionHelperProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = xdg_app_session_helper_proxy_finalize;
  gobject_class->get_property = xdg_app_session_helper_proxy_get_property;
  gobject_class->set_property = xdg_app_session_helper_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = xdg_app_session_helper_proxy_g_signal;
  proxy_class->g_properties_changed = xdg_app_session_helper_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (XdgAppSessionHelperProxyPrivate));
#endif
}

static void
xdg_app_session_helper_proxy_iface_init (XdgAppSessionHelperIface *iface)
{
}

/**
 * xdg_app_session_helper_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-XdgApp-SessionHelper.top_of_page">org.freedesktop.XdgApp.SessionHelper</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call xdg_app_session_helper_proxy_new_finish() to get the result of the operation.
 *
 * See xdg_app_session_helper_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
xdg_app_session_helper_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (XDG_APP_TYPE_SESSION_HELPER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.XdgApp.SessionHelper", NULL);
}

/**
 * xdg_app_session_helper_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to xdg_app_session_helper_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with xdg_app_session_helper_proxy_new().
 *
 * Returns: (transfer full) (type XdgAppSessionHelperProxy): The constructed proxy object or %NULL if @error is set.
 */
XdgAppSessionHelper *
xdg_app_session_helper_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return XDG_APP_SESSION_HELPER (ret);
  else
    return NULL;
}

/**
 * xdg_app_session_helper_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-XdgApp-SessionHelper.top_of_page">org.freedesktop.XdgApp.SessionHelper</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See xdg_app_session_helper_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type XdgAppSessionHelperProxy): The constructed proxy object or %NULL if @error is set.
 */
XdgAppSessionHelper *
xdg_app_session_helper_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (XDG_APP_TYPE_SESSION_HELPER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.XdgApp.SessionHelper", NULL);
  if (ret != NULL)
    return XDG_APP_SESSION_HELPER (ret);
  else
    return NULL;
}


/**
 * xdg_app_session_helper_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like xdg_app_session_helper_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call xdg_app_session_helper_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See xdg_app_session_helper_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
xdg_app_session_helper_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (XDG_APP_TYPE_SESSION_HELPER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.XdgApp.SessionHelper", NULL);
}

/**
 * xdg_app_session_helper_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to xdg_app_session_helper_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with xdg_app_session_helper_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type XdgAppSessionHelperProxy): The constructed proxy object or %NULL if @error is set.
 */
XdgAppSessionHelper *
xdg_app_session_helper_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return XDG_APP_SESSION_HELPER (ret);
  else
    return NULL;
}

/**
 * xdg_app_session_helper_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like xdg_app_session_helper_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See xdg_app_session_helper_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type XdgAppSessionHelperProxy): The constructed proxy object or %NULL if @error is set.
 */
XdgAppSessionHelper *
xdg_app_session_helper_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (XDG_APP_TYPE_SESSION_HELPER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.XdgApp.SessionHelper", NULL);
  if (ret != NULL)
    return XDG_APP_SESSION_HELPER (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * XdgAppSessionHelperSkeleton:
 *
 * The #XdgAppSessionHelperSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * XdgAppSessionHelperSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #XdgAppSessionHelperSkeleton.
 */

struct _XdgAppSessionHelperSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_xdg_app_session_helper_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  XdgAppSessionHelperSkeleton *skeleton = XDG_APP_SESSION_HELPER_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], XDG_APP_TYPE_SESSION_HELPER);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, XDG_APP_TYPE_SESSION_HELPER);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_xdg_app_session_helper_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  XdgAppSessionHelperSkeleton *skeleton = XDG_APP_SESSION_HELPER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_xdg_app_session_helper_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_xdg_app_session_helper_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  XdgAppSessionHelperSkeleton *skeleton = XDG_APP_SESSION_HELPER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_xdg_app_session_helper_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _xdg_app_session_helper_skeleton_vtable =
{
  _xdg_app_session_helper_skeleton_handle_method_call,
  _xdg_app_session_helper_skeleton_handle_get_property,
  _xdg_app_session_helper_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
xdg_app_session_helper_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return xdg_app_session_helper_interface_info ();
}

static GDBusInterfaceVTable *
xdg_app_session_helper_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_xdg_app_session_helper_skeleton_vtable;
}

static GVariant *
xdg_app_session_helper_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  XdgAppSessionHelperSkeleton *skeleton = XDG_APP_SESSION_HELPER_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_xdg_app_session_helper_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _xdg_app_session_helper_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _xdg_app_session_helper_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _xdg_app_session_helper_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.XdgApp.SessionHelper", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
xdg_app_session_helper_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void xdg_app_session_helper_skeleton_iface_init (XdgAppSessionHelperIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (XdgAppSessionHelperSkeleton, xdg_app_session_helper_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (XdgAppSessionHelperSkeleton)
                         G_IMPLEMENT_INTERFACE (XDG_APP_TYPE_SESSION_HELPER, xdg_app_session_helper_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (XdgAppSessionHelperSkeleton, xdg_app_session_helper_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (XDG_APP_TYPE_SESSION_HELPER, xdg_app_session_helper_skeleton_iface_init));

#endif
static void
xdg_app_session_helper_skeleton_finalize (GObject *object)
{
  XdgAppSessionHelperSkeleton *skeleton = XDG_APP_SESSION_HELPER_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (xdg_app_session_helper_skeleton_parent_class)->finalize (object);
}

static void
xdg_app_session_helper_skeleton_init (XdgAppSessionHelperSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = xdg_app_session_helper_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, XDG_APP_TYPE_SESSION_HELPER_SKELETON, XdgAppSessionHelperSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
xdg_app_session_helper_skeleton_class_init (XdgAppSessionHelperSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = xdg_app_session_helper_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = xdg_app_session_helper_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = xdg_app_session_helper_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = xdg_app_session_helper_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = xdg_app_session_helper_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (XdgAppSessionHelperSkeletonPrivate));
#endif
}

static void
xdg_app_session_helper_skeleton_iface_init (XdgAppSessionHelperIface *iface)
{
}

/**
 * xdg_app_session_helper_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-XdgApp-SessionHelper.top_of_page">org.freedesktop.XdgApp.SessionHelper</link>.
 *
 * Returns: (transfer full) (type XdgAppSessionHelperSkeleton): The skeleton object.
 */
XdgAppSessionHelper *
xdg_app_session_helper_skeleton_new (void)
{
  return XDG_APP_SESSION_HELPER (g_object_new (XDG_APP_TYPE_SESSION_HELPER_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.XdgApp.PermissionStore
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:XdgAppPermissionStore
 * @title: XdgAppPermissionStore
 * @short_description: Generated C code for the org.freedesktop.XdgApp.PermissionStore D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-XdgApp-PermissionStore.top_of_page">org.freedesktop.XdgApp.PermissionStore</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.XdgApp.PermissionStore ---- */

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_lookup_IN_ARG_table =
{
  {
    -1,
    (gchar *) "table",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_lookup_IN_ARG_id =
{
  {
    -1,
    (gchar *) "id",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _xdg_app_permission_store_method_info_lookup_IN_ARG_pointers[] =
{
  &_xdg_app_permission_store_method_info_lookup_IN_ARG_table,
  &_xdg_app_permission_store_method_info_lookup_IN_ARG_id,
  NULL
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_lookup_OUT_ARG_permissions =
{
  {
    -1,
    (gchar *) "permissions",
    (gchar *) "a{sas}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_lookup_OUT_ARG_data =
{
  {
    -1,
    (gchar *) "data",
    (gchar *) "v",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _xdg_app_permission_store_method_info_lookup_OUT_ARG_pointers[] =
{
  &_xdg_app_permission_store_method_info_lookup_OUT_ARG_permissions,
  &_xdg_app_permission_store_method_info_lookup_OUT_ARG_data,
  NULL
};

static const _ExtendedGDBusMethodInfo _xdg_app_permission_store_method_info_lookup =
{
  {
    -1,
    (gchar *) "Lookup",
    (GDBusArgInfo **) &_xdg_app_permission_store_method_info_lookup_IN_ARG_pointers,
    (GDBusArgInfo **) &_xdg_app_permission_store_method_info_lookup_OUT_ARG_pointers,
    NULL
  },
  "handle-lookup",
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_IN_ARG_table =
{
  {
    -1,
    (gchar *) "table",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_IN_ARG_create =
{
  {
    -1,
    (gchar *) "create",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_IN_ARG_id =
{
  {
    -1,
    (gchar *) "id",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_IN_ARG_app_permissions =
{
  {
    -1,
    (gchar *) "app_permissions",
    (gchar *) "a{sas}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_IN_ARG_data =
{
  {
    -1,
    (gchar *) "data",
    (gchar *) "v",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _xdg_app_permission_store_method_info_set_IN_ARG_pointers[] =
{
  &_xdg_app_permission_store_method_info_set_IN_ARG_table,
  &_xdg_app_permission_store_method_info_set_IN_ARG_create,
  &_xdg_app_permission_store_method_info_set_IN_ARG_id,
  &_xdg_app_permission_store_method_info_set_IN_ARG_app_permissions,
  &_xdg_app_permission_store_method_info_set_IN_ARG_data,
  NULL
};

static const _ExtendedGDBusMethodInfo _xdg_app_permission_store_method_info_set =
{
  {
    -1,
    (gchar *) "Set",
    (GDBusArgInfo **) &_xdg_app_permission_store_method_info_set_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set",
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_delete_IN_ARG_table =
{
  {
    -1,
    (gchar *) "table",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_delete_IN_ARG_id =
{
  {
    -1,
    (gchar *) "id",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _xdg_app_permission_store_method_info_delete_IN_ARG_pointers[] =
{
  &_xdg_app_permission_store_method_info_delete_IN_ARG_table,
  &_xdg_app_permission_store_method_info_delete_IN_ARG_id,
  NULL
};

static const _ExtendedGDBusMethodInfo _xdg_app_permission_store_method_info_delete =
{
  {
    -1,
    (gchar *) "Delete",
    (GDBusArgInfo **) &_xdg_app_permission_store_method_info_delete_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-delete",
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_value_IN_ARG_table =
{
  {
    -1,
    (gchar *) "table",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_value_IN_ARG_create =
{
  {
    -1,
    (gchar *) "create",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_value_IN_ARG_id =
{
  {
    -1,
    (gchar *) "id",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_value_IN_ARG_data =
{
  {
    -1,
    (gchar *) "data",
    (gchar *) "v",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _xdg_app_permission_store_method_info_set_value_IN_ARG_pointers[] =
{
  &_xdg_app_permission_store_method_info_set_value_IN_ARG_table,
  &_xdg_app_permission_store_method_info_set_value_IN_ARG_create,
  &_xdg_app_permission_store_method_info_set_value_IN_ARG_id,
  &_xdg_app_permission_store_method_info_set_value_IN_ARG_data,
  NULL
};

static const _ExtendedGDBusMethodInfo _xdg_app_permission_store_method_info_set_value =
{
  {
    -1,
    (gchar *) "SetValue",
    (GDBusArgInfo **) &_xdg_app_permission_store_method_info_set_value_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-value",
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_permission_IN_ARG_table =
{
  {
    -1,
    (gchar *) "table",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_permission_IN_ARG_create =
{
  {
    -1,
    (gchar *) "create",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_permission_IN_ARG_id =
{
  {
    -1,
    (gchar *) "id",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_permission_IN_ARG_app =
{
  {
    -1,
    (gchar *) "app",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_set_permission_IN_ARG_permissions =
{
  {
    -1,
    (gchar *) "permissions",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _xdg_app_permission_store_method_info_set_permission_IN_ARG_pointers[] =
{
  &_xdg_app_permission_store_method_info_set_permission_IN_ARG_table,
  &_xdg_app_permission_store_method_info_set_permission_IN_ARG_create,
  &_xdg_app_permission_store_method_info_set_permission_IN_ARG_id,
  &_xdg_app_permission_store_method_info_set_permission_IN_ARG_app,
  &_xdg_app_permission_store_method_info_set_permission_IN_ARG_permissions,
  NULL
};

static const _ExtendedGDBusMethodInfo _xdg_app_permission_store_method_info_set_permission =
{
  {
    -1,
    (gchar *) "SetPermission",
    (GDBusArgInfo **) &_xdg_app_permission_store_method_info_set_permission_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-permission",
  FALSE
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_list_IN_ARG_table =
{
  {
    -1,
    (gchar *) "table",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _xdg_app_permission_store_method_info_list_IN_ARG_pointers[] =
{
  &_xdg_app_permission_store_method_info_list_IN_ARG_table,
  NULL
};

static const _ExtendedGDBusArgInfo _xdg_app_permission_store_method_info_list_OUT_ARG_ids =
{
  {
    -1,
    (gchar *) "ids",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _xdg_app_permission_store_method_info_list_OUT_ARG_pointers[] =
{
  &_xdg_app_permission_store_method_info_list_OUT_ARG_ids,
  NULL
};

static const _ExtendedGDBusMethodInfo _xdg_app_permission_store_method_info_list =
{
  {
    -1,
    (gchar *) "List",
    (GDBusArgInfo **) &_xdg_app_permission_store_method_info_list_IN_ARG_pointers,
    (GDBusArgInfo **) &_xdg_app_permission_store_method_info_list_OUT_ARG_pointers,
    NULL
  },
  "handle-list",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _xdg_app_permission_store_method_info_pointers[] =
{
  &_xdg_app_permission_store_method_info_lookup,
  &_xdg_app_permission_store_method_info_set,
  &_xdg_app_permission_store_method_info_delete,
  &_xdg_app_permission_store_method_info_set_value,
  &_xdg_app_permission_store_method_info_set_permission,
  &_xdg_app_permission_store_method_info_list,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _xdg_app_permission_store_interface_info =
{
  {
    -1,
    (gchar *) "org.freedesktop.XdgApp.PermissionStore",
    (GDBusMethodInfo **) &_xdg_app_permission_store_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "permission-store",
};


/**
 * xdg_app_permission_store_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-XdgApp-PermissionStore.top_of_page">org.freedesktop.XdgApp.PermissionStore</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
xdg_app_permission_store_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_xdg_app_permission_store_interface_info.parent_struct;
}

/**
 * xdg_app_permission_store_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #XdgAppPermissionStore interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
xdg_app_permission_store_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * XdgAppPermissionStore:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-XdgApp-PermissionStore.top_of_page">org.freedesktop.XdgApp.PermissionStore</link>.
 */

/**
 * XdgAppPermissionStoreIface:
 * @parent_iface: The parent interface.
 * @handle_delete: Handler for the #XdgAppPermissionStore::handle-delete signal.
 * @handle_list: Handler for the #XdgAppPermissionStore::handle-list signal.
 * @handle_lookup: Handler for the #XdgAppPermissionStore::handle-lookup signal.
 * @handle_set: Handler for the #XdgAppPermissionStore::handle-set signal.
 * @handle_set_permission: Handler for the #XdgAppPermissionStore::handle-set-permission signal.
 * @handle_set_value: Handler for the #XdgAppPermissionStore::handle-set-value signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-XdgApp-PermissionStore.top_of_page">org.freedesktop.XdgApp.PermissionStore</link>.
 */

typedef XdgAppPermissionStoreIface XdgAppPermissionStoreInterface;
G_DEFINE_INTERFACE (XdgAppPermissionStore, xdg_app_permission_store, G_TYPE_OBJECT);

static void
xdg_app_permission_store_default_init (XdgAppPermissionStoreIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * XdgAppPermissionStore::handle-lookup:
   * @object: A #XdgAppPermissionStore.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_table: Argument passed by remote caller.
   * @arg_id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.Lookup">Lookup()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call xdg_app_permission_store_complete_lookup() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-lookup",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (XdgAppPermissionStoreIface, handle_lookup),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * XdgAppPermissionStore::handle-set:
   * @object: A #XdgAppPermissionStore.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_table: Argument passed by remote caller.
   * @arg_create: Argument passed by remote caller.
   * @arg_id: Argument passed by remote caller.
   * @arg_app_permissions: Argument passed by remote caller.
   * @arg_data: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.Set">Set()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call xdg_app_permission_store_complete_set() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (XdgAppPermissionStoreIface, handle_set),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    6,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_BOOLEAN, G_TYPE_STRING, G_TYPE_VARIANT, G_TYPE_VARIANT);

  /**
   * XdgAppPermissionStore::handle-delete:
   * @object: A #XdgAppPermissionStore.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_table: Argument passed by remote caller.
   * @arg_id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.Delete">Delete()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call xdg_app_permission_store_complete_delete() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-delete",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (XdgAppPermissionStoreIface, handle_delete),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * XdgAppPermissionStore::handle-set-value:
   * @object: A #XdgAppPermissionStore.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_table: Argument passed by remote caller.
   * @arg_create: Argument passed by remote caller.
   * @arg_id: Argument passed by remote caller.
   * @arg_data: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.SetValue">SetValue()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call xdg_app_permission_store_complete_set_value() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-value",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (XdgAppPermissionStoreIface, handle_set_value),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    5,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_BOOLEAN, G_TYPE_STRING, G_TYPE_VARIANT);

  /**
   * XdgAppPermissionStore::handle-set-permission:
   * @object: A #XdgAppPermissionStore.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_table: Argument passed by remote caller.
   * @arg_create: Argument passed by remote caller.
   * @arg_id: Argument passed by remote caller.
   * @arg_app: Argument passed by remote caller.
   * @arg_permissions: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.SetPermission">SetPermission()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call xdg_app_permission_store_complete_set_permission() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-permission",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (XdgAppPermissionStoreIface, handle_set_permission),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    6,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_BOOLEAN, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRV);

  /**
   * XdgAppPermissionStore::handle-list:
   * @object: A #XdgAppPermissionStore.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_table: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.List">List()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call xdg_app_permission_store_complete_list() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-list",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (XdgAppPermissionStoreIface, handle_list),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

}

/**
 * xdg_app_permission_store_call_lookup:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @arg_table: Argument to pass with the method invocation.
 * @arg_id: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.Lookup">Lookup()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call xdg_app_permission_store_call_lookup_finish() to get the result of the operation.
 *
 * See xdg_app_permission_store_call_lookup_sync() for the synchronous, blocking version of this method.
 */
void
xdg_app_permission_store_call_lookup (
    XdgAppPermissionStore *proxy,
    const gchar *arg_table,
    const gchar *arg_id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Lookup",
    g_variant_new ("(ss)",
                   arg_table,
                   arg_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * xdg_app_permission_store_call_lookup_finish:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @out_permissions: (out): Return location for return parameter or %NULL to ignore.
 * @out_data: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to xdg_app_permission_store_call_lookup().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with xdg_app_permission_store_call_lookup().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_permission_store_call_lookup_finish (
    XdgAppPermissionStore *proxy,
    GVariant **out_permissions,
    GVariant **out_data,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a{sas}@v)",
                 out_permissions,
                 out_data);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_permission_store_call_lookup_sync:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @arg_table: Argument to pass with the method invocation.
 * @arg_id: Argument to pass with the method invocation.
 * @out_permissions: (out): Return location for return parameter or %NULL to ignore.
 * @out_data: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.Lookup">Lookup()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See xdg_app_permission_store_call_lookup() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_permission_store_call_lookup_sync (
    XdgAppPermissionStore *proxy,
    const gchar *arg_table,
    const gchar *arg_id,
    GVariant **out_permissions,
    GVariant **out_data,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Lookup",
    g_variant_new ("(ss)",
                   arg_table,
                   arg_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a{sas}@v)",
                 out_permissions,
                 out_data);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_permission_store_call_set:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @arg_table: Argument to pass with the method invocation.
 * @arg_create: Argument to pass with the method invocation.
 * @arg_id: Argument to pass with the method invocation.
 * @arg_app_permissions: Argument to pass with the method invocation.
 * @arg_data: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.Set">Set()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call xdg_app_permission_store_call_set_finish() to get the result of the operation.
 *
 * See xdg_app_permission_store_call_set_sync() for the synchronous, blocking version of this method.
 */
void
xdg_app_permission_store_call_set (
    XdgAppPermissionStore *proxy,
    const gchar *arg_table,
    gboolean arg_create,
    const gchar *arg_id,
    GVariant *arg_app_permissions,
    GVariant *arg_data,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Set",
    g_variant_new ("(sbs@a{sas}@v)",
                   arg_table,
                   arg_create,
                   arg_id,
                   arg_app_permissions,
                   arg_data),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * xdg_app_permission_store_call_set_finish:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to xdg_app_permission_store_call_set().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with xdg_app_permission_store_call_set().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_permission_store_call_set_finish (
    XdgAppPermissionStore *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_permission_store_call_set_sync:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @arg_table: Argument to pass with the method invocation.
 * @arg_create: Argument to pass with the method invocation.
 * @arg_id: Argument to pass with the method invocation.
 * @arg_app_permissions: Argument to pass with the method invocation.
 * @arg_data: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.Set">Set()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See xdg_app_permission_store_call_set() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_permission_store_call_set_sync (
    XdgAppPermissionStore *proxy,
    const gchar *arg_table,
    gboolean arg_create,
    const gchar *arg_id,
    GVariant *arg_app_permissions,
    GVariant *arg_data,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Set",
    g_variant_new ("(sbs@a{sas}@v)",
                   arg_table,
                   arg_create,
                   arg_id,
                   arg_app_permissions,
                   arg_data),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_permission_store_call_delete:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @arg_table: Argument to pass with the method invocation.
 * @arg_id: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.Delete">Delete()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call xdg_app_permission_store_call_delete_finish() to get the result of the operation.
 *
 * See xdg_app_permission_store_call_delete_sync() for the synchronous, blocking version of this method.
 */
void
xdg_app_permission_store_call_delete (
    XdgAppPermissionStore *proxy,
    const gchar *arg_table,
    const gchar *arg_id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Delete",
    g_variant_new ("(ss)",
                   arg_table,
                   arg_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * xdg_app_permission_store_call_delete_finish:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to xdg_app_permission_store_call_delete().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with xdg_app_permission_store_call_delete().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_permission_store_call_delete_finish (
    XdgAppPermissionStore *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_permission_store_call_delete_sync:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @arg_table: Argument to pass with the method invocation.
 * @arg_id: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.Delete">Delete()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See xdg_app_permission_store_call_delete() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_permission_store_call_delete_sync (
    XdgAppPermissionStore *proxy,
    const gchar *arg_table,
    const gchar *arg_id,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Delete",
    g_variant_new ("(ss)",
                   arg_table,
                   arg_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_permission_store_call_set_value:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @arg_table: Argument to pass with the method invocation.
 * @arg_create: Argument to pass with the method invocation.
 * @arg_id: Argument to pass with the method invocation.
 * @arg_data: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.SetValue">SetValue()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call xdg_app_permission_store_call_set_value_finish() to get the result of the operation.
 *
 * See xdg_app_permission_store_call_set_value_sync() for the synchronous, blocking version of this method.
 */
void
xdg_app_permission_store_call_set_value (
    XdgAppPermissionStore *proxy,
    const gchar *arg_table,
    gboolean arg_create,
    const gchar *arg_id,
    GVariant *arg_data,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetValue",
    g_variant_new ("(sbs@v)",
                   arg_table,
                   arg_create,
                   arg_id,
                   arg_data),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * xdg_app_permission_store_call_set_value_finish:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to xdg_app_permission_store_call_set_value().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with xdg_app_permission_store_call_set_value().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_permission_store_call_set_value_finish (
    XdgAppPermissionStore *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_permission_store_call_set_value_sync:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @arg_table: Argument to pass with the method invocation.
 * @arg_create: Argument to pass with the method invocation.
 * @arg_id: Argument to pass with the method invocation.
 * @arg_data: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.SetValue">SetValue()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See xdg_app_permission_store_call_set_value() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_permission_store_call_set_value_sync (
    XdgAppPermissionStore *proxy,
    const gchar *arg_table,
    gboolean arg_create,
    const gchar *arg_id,
    GVariant *arg_data,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetValue",
    g_variant_new ("(sbs@v)",
                   arg_table,
                   arg_create,
                   arg_id,
                   arg_data),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_permission_store_call_set_permission:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @arg_table: Argument to pass with the method invocation.
 * @arg_create: Argument to pass with the method invocation.
 * @arg_id: Argument to pass with the method invocation.
 * @arg_app: Argument to pass with the method invocation.
 * @arg_permissions: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.SetPermission">SetPermission()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call xdg_app_permission_store_call_set_permission_finish() to get the result of the operation.
 *
 * See xdg_app_permission_store_call_set_permission_sync() for the synchronous, blocking version of this method.
 */
void
xdg_app_permission_store_call_set_permission (
    XdgAppPermissionStore *proxy,
    const gchar *arg_table,
    gboolean arg_create,
    const gchar *arg_id,
    const gchar *arg_app,
    const gchar *const *arg_permissions,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetPermission",
    g_variant_new ("(sbss^as)",
                   arg_table,
                   arg_create,
                   arg_id,
                   arg_app,
                   arg_permissions),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * xdg_app_permission_store_call_set_permission_finish:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to xdg_app_permission_store_call_set_permission().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with xdg_app_permission_store_call_set_permission().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_permission_store_call_set_permission_finish (
    XdgAppPermissionStore *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_permission_store_call_set_permission_sync:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @arg_table: Argument to pass with the method invocation.
 * @arg_create: Argument to pass with the method invocation.
 * @arg_id: Argument to pass with the method invocation.
 * @arg_app: Argument to pass with the method invocation.
 * @arg_permissions: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.SetPermission">SetPermission()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See xdg_app_permission_store_call_set_permission() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_permission_store_call_set_permission_sync (
    XdgAppPermissionStore *proxy,
    const gchar *arg_table,
    gboolean arg_create,
    const gchar *arg_id,
    const gchar *arg_app,
    const gchar *const *arg_permissions,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetPermission",
    g_variant_new ("(sbss^as)",
                   arg_table,
                   arg_create,
                   arg_id,
                   arg_app,
                   arg_permissions),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_permission_store_call_list:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @arg_table: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.List">List()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call xdg_app_permission_store_call_list_finish() to get the result of the operation.
 *
 * See xdg_app_permission_store_call_list_sync() for the synchronous, blocking version of this method.
 */
void
xdg_app_permission_store_call_list (
    XdgAppPermissionStore *proxy,
    const gchar *arg_table,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "List",
    g_variant_new ("(s)",
                   arg_table),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * xdg_app_permission_store_call_list_finish:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @out_ids: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to xdg_app_permission_store_call_list().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with xdg_app_permission_store_call_list().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_permission_store_call_list_finish (
    XdgAppPermissionStore *proxy,
    gchar ***out_ids,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(^as)",
                 out_ids);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_permission_store_call_list_sync:
 * @proxy: A #XdgAppPermissionStoreProxy.
 * @arg_table: Argument to pass with the method invocation.
 * @out_ids: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.List">List()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See xdg_app_permission_store_call_list() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
xdg_app_permission_store_call_list_sync (
    XdgAppPermissionStore *proxy,
    const gchar *arg_table,
    gchar ***out_ids,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "List",
    g_variant_new ("(s)",
                   arg_table),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(^as)",
                 out_ids);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * xdg_app_permission_store_complete_lookup:
 * @object: A #XdgAppPermissionStore.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @permissions: Parameter to return.
 * @data: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.Lookup">Lookup()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
xdg_app_permission_store_complete_lookup (
    XdgAppPermissionStore *object,
    GDBusMethodInvocation *invocation,
    GVariant *permissions,
    GVariant *data)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a{sas}@v)",
                   permissions,
                   data));
}

/**
 * xdg_app_permission_store_complete_set:
 * @object: A #XdgAppPermissionStore.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.Set">Set()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
xdg_app_permission_store_complete_set (
    XdgAppPermissionStore *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * xdg_app_permission_store_complete_delete:
 * @object: A #XdgAppPermissionStore.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.Delete">Delete()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
xdg_app_permission_store_complete_delete (
    XdgAppPermissionStore *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * xdg_app_permission_store_complete_set_value:
 * @object: A #XdgAppPermissionStore.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.SetValue">SetValue()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
xdg_app_permission_store_complete_set_value (
    XdgAppPermissionStore *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * xdg_app_permission_store_complete_set_permission:
 * @object: A #XdgAppPermissionStore.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.SetPermission">SetPermission()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
xdg_app_permission_store_complete_set_permission (
    XdgAppPermissionStore *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * xdg_app_permission_store_complete_list:
 * @object: A #XdgAppPermissionStore.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @ids: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-XdgApp-PermissionStore.List">List()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
xdg_app_permission_store_complete_list (
    XdgAppPermissionStore *object,
    GDBusMethodInvocation *invocation,
    const gchar *const *ids)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(^as)",
                   ids));
}

/* ------------------------------------------------------------------------ */

/**
 * XdgAppPermissionStoreProxy:
 *
 * The #XdgAppPermissionStoreProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * XdgAppPermissionStoreProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #XdgAppPermissionStoreProxy.
 */

struct _XdgAppPermissionStoreProxyPrivate
{
  GData *qdata;
};

static void xdg_app_permission_store_proxy_iface_init (XdgAppPermissionStoreIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (XdgAppPermissionStoreProxy, xdg_app_permission_store_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (XdgAppPermissionStoreProxy)
                         G_IMPLEMENT_INTERFACE (XDG_APP_TYPE_PERMISSION_STORE, xdg_app_permission_store_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (XdgAppPermissionStoreProxy, xdg_app_permission_store_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (XDG_APP_TYPE_PERMISSION_STORE, xdg_app_permission_store_proxy_iface_init));

#endif
static void
xdg_app_permission_store_proxy_finalize (GObject *object)
{
  XdgAppPermissionStoreProxy *proxy = XDG_APP_PERMISSION_STORE_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (xdg_app_permission_store_proxy_parent_class)->finalize (object);
}

static void
xdg_app_permission_store_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
xdg_app_permission_store_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
xdg_app_permission_store_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_xdg_app_permission_store_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], XDG_APP_TYPE_PERMISSION_STORE);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, XDG_APP_TYPE_PERMISSION_STORE);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
xdg_app_permission_store_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  XdgAppPermissionStoreProxy *proxy = XDG_APP_PERMISSION_STORE_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_xdg_app_permission_store_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_xdg_app_permission_store_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
xdg_app_permission_store_proxy_init (XdgAppPermissionStoreProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = xdg_app_permission_store_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, XDG_APP_TYPE_PERMISSION_STORE_PROXY, XdgAppPermissionStoreProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), xdg_app_permission_store_interface_info ());
}

static void
xdg_app_permission_store_proxy_class_init (XdgAppPermissionStoreProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = xdg_app_permission_store_proxy_finalize;
  gobject_class->get_property = xdg_app_permission_store_proxy_get_property;
  gobject_class->set_property = xdg_app_permission_store_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = xdg_app_permission_store_proxy_g_signal;
  proxy_class->g_properties_changed = xdg_app_permission_store_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (XdgAppPermissionStoreProxyPrivate));
#endif
}

static void
xdg_app_permission_store_proxy_iface_init (XdgAppPermissionStoreIface *iface)
{
}

/**
 * xdg_app_permission_store_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-XdgApp-PermissionStore.top_of_page">org.freedesktop.XdgApp.PermissionStore</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call xdg_app_permission_store_proxy_new_finish() to get the result of the operation.
 *
 * See xdg_app_permission_store_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
xdg_app_permission_store_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (XDG_APP_TYPE_PERMISSION_STORE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.XdgApp.PermissionStore", NULL);
}

/**
 * xdg_app_permission_store_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to xdg_app_permission_store_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with xdg_app_permission_store_proxy_new().
 *
 * Returns: (transfer full) (type XdgAppPermissionStoreProxy): The constructed proxy object or %NULL if @error is set.
 */
XdgAppPermissionStore *
xdg_app_permission_store_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return XDG_APP_PERMISSION_STORE (ret);
  else
    return NULL;
}

/**
 * xdg_app_permission_store_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-XdgApp-PermissionStore.top_of_page">org.freedesktop.XdgApp.PermissionStore</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See xdg_app_permission_store_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type XdgAppPermissionStoreProxy): The constructed proxy object or %NULL if @error is set.
 */
XdgAppPermissionStore *
xdg_app_permission_store_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (XDG_APP_TYPE_PERMISSION_STORE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.XdgApp.PermissionStore", NULL);
  if (ret != NULL)
    return XDG_APP_PERMISSION_STORE (ret);
  else
    return NULL;
}


/**
 * xdg_app_permission_store_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like xdg_app_permission_store_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call xdg_app_permission_store_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See xdg_app_permission_store_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
xdg_app_permission_store_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (XDG_APP_TYPE_PERMISSION_STORE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.XdgApp.PermissionStore", NULL);
}

/**
 * xdg_app_permission_store_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to xdg_app_permission_store_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with xdg_app_permission_store_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type XdgAppPermissionStoreProxy): The constructed proxy object or %NULL if @error is set.
 */
XdgAppPermissionStore *
xdg_app_permission_store_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return XDG_APP_PERMISSION_STORE (ret);
  else
    return NULL;
}

/**
 * xdg_app_permission_store_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like xdg_app_permission_store_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See xdg_app_permission_store_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type XdgAppPermissionStoreProxy): The constructed proxy object or %NULL if @error is set.
 */
XdgAppPermissionStore *
xdg_app_permission_store_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (XDG_APP_TYPE_PERMISSION_STORE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.XdgApp.PermissionStore", NULL);
  if (ret != NULL)
    return XDG_APP_PERMISSION_STORE (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * XdgAppPermissionStoreSkeleton:
 *
 * The #XdgAppPermissionStoreSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * XdgAppPermissionStoreSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #XdgAppPermissionStoreSkeleton.
 */

struct _XdgAppPermissionStoreSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_xdg_app_permission_store_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  XdgAppPermissionStoreSkeleton *skeleton = XDG_APP_PERMISSION_STORE_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], XDG_APP_TYPE_PERMISSION_STORE);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, XDG_APP_TYPE_PERMISSION_STORE);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_xdg_app_permission_store_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  XdgAppPermissionStoreSkeleton *skeleton = XDG_APP_PERMISSION_STORE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_xdg_app_permission_store_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_xdg_app_permission_store_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  XdgAppPermissionStoreSkeleton *skeleton = XDG_APP_PERMISSION_STORE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_xdg_app_permission_store_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _xdg_app_permission_store_skeleton_vtable =
{
  _xdg_app_permission_store_skeleton_handle_method_call,
  _xdg_app_permission_store_skeleton_handle_get_property,
  _xdg_app_permission_store_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
xdg_app_permission_store_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return xdg_app_permission_store_interface_info ();
}

static GDBusInterfaceVTable *
xdg_app_permission_store_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_xdg_app_permission_store_skeleton_vtable;
}

static GVariant *
xdg_app_permission_store_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  XdgAppPermissionStoreSkeleton *skeleton = XDG_APP_PERMISSION_STORE_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_xdg_app_permission_store_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _xdg_app_permission_store_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _xdg_app_permission_store_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _xdg_app_permission_store_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.XdgApp.PermissionStore", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
xdg_app_permission_store_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void xdg_app_permission_store_skeleton_iface_init (XdgAppPermissionStoreIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (XdgAppPermissionStoreSkeleton, xdg_app_permission_store_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (XdgAppPermissionStoreSkeleton)
                         G_IMPLEMENT_INTERFACE (XDG_APP_TYPE_PERMISSION_STORE, xdg_app_permission_store_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (XdgAppPermissionStoreSkeleton, xdg_app_permission_store_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (XDG_APP_TYPE_PERMISSION_STORE, xdg_app_permission_store_skeleton_iface_init));

#endif
static void
xdg_app_permission_store_skeleton_finalize (GObject *object)
{
  XdgAppPermissionStoreSkeleton *skeleton = XDG_APP_PERMISSION_STORE_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (xdg_app_permission_store_skeleton_parent_class)->finalize (object);
}

static void
xdg_app_permission_store_skeleton_init (XdgAppPermissionStoreSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = xdg_app_permission_store_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, XDG_APP_TYPE_PERMISSION_STORE_SKELETON, XdgAppPermissionStoreSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
xdg_app_permission_store_skeleton_class_init (XdgAppPermissionStoreSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = xdg_app_permission_store_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = xdg_app_permission_store_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = xdg_app_permission_store_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = xdg_app_permission_store_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = xdg_app_permission_store_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (XdgAppPermissionStoreSkeletonPrivate));
#endif
}

static void
xdg_app_permission_store_skeleton_iface_init (XdgAppPermissionStoreIface *iface)
{
}

/**
 * xdg_app_permission_store_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-XdgApp-PermissionStore.top_of_page">org.freedesktop.XdgApp.PermissionStore</link>.
 *
 * Returns: (transfer full) (type XdgAppPermissionStoreSkeleton): The skeleton object.
 */
XdgAppPermissionStore *
xdg_app_permission_store_skeleton_new (void)
{
  return XDG_APP_PERMISSION_STORE (g_object_new (XDG_APP_TYPE_PERMISSION_STORE_SKELETON, NULL));
}

