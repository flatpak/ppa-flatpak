From: Alexander Larsson <alexl@redhat.com>
Date: Mon, 16 Dec 2019 21:14:41 +0100
Subject: dir: Fix leak in flatpak_dir_p2p_state_free

We forgot to free the results_refs member

Origin: https://github.com/flatpak/flatpak/pull/3307
---
 common/flatpak-dir.c | 3 +++
 1 file changed, 3 insertions(+)

diff --git a/common/flatpak-dir.c b/common/flatpak-dir.c
index bd9d094..8756bd3 100644
--- a/common/flatpak-dir.c
+++ b/common/flatpak-dir.c
@@ -3865,6 +3865,9 @@ flatpak_dir_p2p_state_free (FlatpakDirP2PState *state)
   if (state->results)
     g_ptr_array_unref (state->results);
 
+  if (state->results_refs)
+    g_ptr_array_unref (state->results_refs);
+
   g_free (state);
 }
 
