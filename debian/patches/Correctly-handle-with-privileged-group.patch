From: Alexander Larsson <alexl@redhat.com>
Date: Tue, 24 May 2016 14:13:34 +0200
Subject: Correctly handle --with-privileged-group

Origin: upstream, 0.6.3, commit:cb41e1bdba74240fc9bda5ea9d95f82928113db7
---
 system-helper/org.freedesktop.Flatpak.rules.in | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/system-helper/org.freedesktop.Flatpak.rules.in b/system-helper/org.freedesktop.Flatpak.rules.in
index c1ce8ee..4b8be53 100644
--- a/system-helper/org.freedesktop.Flatpak.rules.in
+++ b/system-helper/org.freedesktop.Flatpak.rules.in
@@ -2,7 +2,7 @@ polkit.addRule(function(action, subject) {
     if ((action.id == "org.freedesktop.Flatpak.app-install" ||
          action.id == "org.freedesktop.Flatpak.runtime-install") &&
         subject.active == true && subject.local == true &&
-        subject.isInGroup("wheel")) {
+        subject.isInGroup("@privileged_group@")) {
             return polkit.Result.YES;
     }
 });
